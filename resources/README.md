# resources/

Static assets used at runtime: Cyrillic-capable fonts and document templates
for PDF/DOCX export (spec §10), plus MOST Partners branding when provided.

Populated alongside the export feature (Phase 4). Empty in step 0.1.
