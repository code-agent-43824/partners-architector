# tests/

Cross-cutting integration tests (API + database). Unit tests live next to the
code they cover inside each workspace (e.g. `packages/shared/src/*.test.ts`).

Populated as integration-worthy behavior appears (from step 0.6 onward).
Empty in step 0.1.
