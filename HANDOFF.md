# HANDOFF

Shared, living handoff document for the coding agents working on Partners Architector.

Any agent's session can stop at any time. This file is the single source of truth for what is in progress, so another agent can resume without losing context. The rules live in the "Collaboration and handoff" section of `AGENTS.md`. In short: write down what you are about to do here and commit it **before** you start, keep it updated as you go, and record the result here when you finish.

**Last updated:** 2026-07-03 — by: Watson

## Current status

PHASE 1 (complete and live) — Steps **1.1–1.5d are live** on the Oracle host (Podman/Caddy, `https://partners-architector.duckdns.org/`). The current live release is application commit `5e26bb1`; active images are `localhost/psa-api:5e26bb1` and `localhost/psa-web:4d4766d`; `psa-db`, `psa_pgdata`, and schema were not changed during the W2/O1 deploy. The live question seed now contains `mvp-draft-1` and `mvp-draft-2` (30 rows each); new sessions instantiate `mvp-draft-2`, existing sessions keep their old wording. Phase 1 now covers partnership CRUD, partners, sessions, the 30-block scenario engine, formulation capture/autosave, partner sign-offs, formulation version history + rollback, and the TipTap/ProseMirror rich formulation editor. Unit tests, local build checks, public health checks, and production feature smokes are green. **Development has been re-planned around a demo to Dmitry Gritz** (owner decision 2026-07-02): next is the **Demo readiness plan (D1–D5)** — session-day UX, manual shares input, agreement assembly + PDF/DOCX export, demo kit — see `docs/plans/demo-readiness.md`; the Gritz Calculator (Phase 2) and the authority matrix (Phase 3) move to **after** the demo feedback. In parallel, **Watson owns a standing ops/security backlog (W1–W6)** — backups, registration gating, password change, committed e2e suite, HTML sanitization, host health cron — see "Watson backlog" below. Spec `docs/spec/psa-mvp.md`; plans `docs/plans/demo-readiness.md`, `docs/plans/phase-1-cases-sessions-scenario-capture.md`; ADRs 0001, 0002.

### Owner decisions (2026-06-22)
- "First stage" = **Phase 0** (skeleton). Confirmed.
- **Do not rename the repository.** Build in `partners-architector`; product codename stays `psa` as in the spec.
- **Dev-server constraint:** the current development server is far from the target hardware — **do not attempt to run/deploy any LLM on it.** Phase 0 involves no LLM anyway (AI/ASR are Phases 7–8). The owner will provide a separate server when we reach the LLM phases.
- Notify the owner when Watson (deploy agent) is needed; not required during Phase 0.
- **Two deploy targets (2026-06-23):** keep both Docker Compose and Podman as supported deploy targets (ADR 0002). Oracle currently uses Podman; a future host may use Docker — keep `deploy/docker-compose.yml` and `deploy/podman/` in sync.
- **Partner count bounds (2026-06-24):** a partnership may have **up to 5 partners** — a hard cap enforced when adding. The **minimum of 2 is not enforced** while editing partners (a partnership may temporarily have 0–1); it is validated later, when a session is started (step 1.3). This widens the spec's FR-2.3 upper bound ("2–4") to 5 per the owner's instruction.
- **Split step 1.5 (2026-06-25):** capture is delivered as sub-steps **1.5a–d**, each independently committed/deployed (1.5a text+rationale+autosave → 1.5b partner sign-offs → 1.5c version history+rollback → 1.5d TipTap editor). The formulation editor starts as a plain `textarea`; the TipTap/ProseMirror rich editor (spec §7.2, a new front-end dependency) is the final sub-step 1.5d.
- **Clause version trigger (2026-06-30):** snapshot a `clause_version` before significant events — on the transition to «согласовано», on an explicit «сохранить версию» button, and before a rollback (so a restore is non-destructive). **Not** on every autosave keystroke (would create too many versions). Per the owner ("как рекомендовано").
- **Demo-first re-plan (2026-07-02):** development is re-sequenced around a demo to Dmitry Gritz and the architects (goal: directional feedback while already delivering real value; the product must not read as a prototype). Owner's accepted hypothesis: near-term value is **routine removal + assembling the agreement**, not the calculator (shares can be computed on paper). Therefore: parts of spec Phase 4 (agreement assembly + PDF/DOCX export) are pulled forward; **Phase 2 (Gritz Calculator) and Phase 3 (matrix) move to after the demo feedback**; the shares block gets the spec's manual input mode (FR-5.1) as interim; the calculator's place is *shown* in the demo as the next milestone. Plan: `docs/plans/demo-readiness.md`. Additionally, the owner handed **Watson** the ops/security/e2e backlog (W1–W6 below) — deploy-adjacent and security work found in the 2026-07-02 CTO/PM review.

## Active task

### Demo readiness — прототип для показа Грицу (planned; D1 awaiting owner go)
- Owner: code-writing agent (Claude) — Plan committed: 2026-07-02. Full detail: `docs/plans/demo-readiness.md`.
- Goal: show Gritz + architects a product that already removes routine and assembles the partnership agreement — directional feedback without looking like a prototype.
- Plan (ordered, each step independently committable + deployable):
  - [ ] D1 Session-day UX: FR-3.2 navigation (TOC with status indicators + single-block focus + prev/next + keyboard), proper modal dialogs (replace `window.confirm/prompt`), Russian specific error messages + connection-lost indicator, visual polish pass, autosave via `setQueryData` (no full-list refetch).
  - [ ] D2 Shares block manual input (FR-5.1 manual mode + FR-5.8 «смысл долей») → `clause.structured_data`; sum=100% live check; calculator mode visible as «скоро».
  - [ ] D3 Agreement assembly (DOC-1 minus matrix: title page + Принципы + 30 sections + shares table) as a document-styled page with print CSS («Печать/PDF» via browser).
  - [ ] D4 Server export PDF (Playwright/Chromium) + DOCX (`docx` lib) + `export_record`; needs W5 (sanitization) first; adds heavy api-image deps — coordinate the deploy with Watson.
  - [ ] D5 Demo kit: idempotent `db:seed:demo` (realistic filled case), 10–12 min demo script, dry run + screenshots to the owner.
- Owner tasks: **O1 DONE (2026-07-02, by Claude; deployed by Watson 2026-07-02)** — the 30 block texts were revised against the owner-provided methodology workbook («Шаблон партнёрского соглашения», fetched from a temporary link on the prod host; NOT committed — proprietary material, public repo). `QUESTION_SET_VERSION` bumped `mvp-draft-1` → `mvp-draft-2`; structure (order, contours, heavy flags) kept per spec Appendix A (the workbook carries no per-question contour/heavy markers). Titles now match the workbook verbatim (fuller №8/№14/№16/№19/№20/№21/№22/№25/№26/№27/№30; parentheticals dropped from №6/№7); prompts use the workbook's own imperative instructions; helper questions use the workbook's guiding bullets. **Findings for the owner/Gritz demo:** (1) №2 semantics corrected — it is «видение + миссия компании», not "personal why" as drafted before; (2) in the workbook the **authority matrix belongs to question №15** (№16 covers procedures) — Phase 3's matrix UI should attach to block 15, and the spec's "(матрица)" label on №16 is a spec-side inaccuracy; (3) №5 carries the caveat that the Gritz Calculator is *contraindicated* for redistributing shares in existing businesses — reinforces FR-5.2 for D2/Phase 2; (4) the workbook is designed for up to 3 partners (with printable templates for 4–5) — consistent with our max-5 cap. Live seed now has 30 rows each for `mvp-draft-1` and `mvp-draft-2`; a production smoke verified a newly created session gets 30 clauses and the new first prompt («Напишите имена и фамилии людей…»). Still pending Gritz's personal sign-off — that is the demo's purpose. **Cleanup done:** Watson removed the temporary methodology PDF from the prod web container during the W2 deploy. O2 demo timing/audience; O3 matrix before demo? (default: after; note finding (2) above); O4 review D1 screenshots; O5 demo after D4 (recommended) or D3.
- After the demo: feedback re-prioritizes Phase 2 (calculator) → 3 (matrix) → rest of 4 (legalization checklist) → 5 → 6 → 7–8.

### Intermediate deploy — O1 question texts `mvp-draft-2` (COMPLETE)
- Owner: Watson — Handoff committed: 2026-07-02; deployed: 2026-07-02 with W2
- Source: the O1 commit on `main` — content-only change to `apps/api/src/seed/questions.ts` (revised texts + `QUESTION_SET_VERSION = 'mvp-draft-2'`). No schema change, no API change, no web change, no new deps.
- Plan: rebuild + recreate `psa-api` only (`psa-web` and `psa-db` untouched), then run the one-time seed: `podman exec psa-api pnpm --filter @psa/api db:seed` — **the first re-seed since Phase 0.** It inserts 30 new `question` rows under `mvp-draft-2` (old `mvp-draft-1` rows remain; existing sessions keep their old wording by design — new sessions instantiate the new texts).
- Verify: seed prints `Seed complete: 30 questions; settings already present.`; a **newly created** session's first block shows the new prompt («Напишите имена и фамилии людей…»); `SELECT question_set_version, count(*) FROM question GROUP BY 1` shows 30 rows per version. Also delete the temporary `/_tmp/methodology-….pdf` from the host (publicly reachable proprietary file).
- Note: verified locally on a disposable DB (migrate → seed ×2 idempotent → 30/10-heavy rows under `mvp-draft-2`).
- Result: deployed API image `localhost/psa-api:5e26bb1`, ran the one-time `db:seed`, and verified `mvp-draft-1|30` plus `mvp-draft-2|30`. Temporary methodology PDF was removed from `psa-web`; the old public URL no longer serves a PDF.

### Watson backlog — ops, security, e2e (handed to Watson 2026-07-02)
Standing backlog from the owner (sourced from the 2026-07-02 CTO/PM review); work through it in parallel with the demo plan, small commits, HANDOFF discipline per item. W1 first — it protects real data.
- [x] **W1 Backups (NFR-3, top priority) — COMPLETE by Watson 2026-07-02:** nightly `pg_dump` of `psa-db` on the Oracle host (e.g. cron → `/opt/partners-architector/backups`, rotate ≥14 days), a documented restore procedure in `deploy/README.md`, and **one tested restore** (into a scratch database/container — never the live one). Also check and report the disk-encryption status of the DB volume (SEC-6); no secrets committed.
  - Result: installed `/opt/partners-architector/bin/backup-db.sh` (root-owned, mode `0750`) and `/etc/cron.d/partners-architector-db-backup` (daily 02:17 UTC). Backups are written to `/opt/partners-architector/backups` (root-owned, mode `0700`) as custom-format dumps plus `.sha256`, with retention of files older than 14 days. First backup `psa-20260702T174258Z.dump` completed and checksum verified. Restored that dump into a disposable `psa-restore-test-*` Postgres/pgvector container on `psa-net`; table counts matched live (`question=30`, `account=5`, `partnership=2`, `session=2`, `clause=60`, etc.); scratch container removed. Live `psa-db` schema/data were not modified. Restore procedure documented in `deploy/README.md`. Disk-encryption finding: DB volume is under `/var/lib/containers/storage/volumes/psa_pgdata` on `/dev/mapper/ocivolume-root` (`xfs`) backed by plain LVM linear mapping; no guest-visible `crypto_LUKS`/dm-crypt layer. OCI-managed block-volume encryption, if needed for SEC-6, must be verified in the OCI console because it is not observable from the guest.
- [x] **W2 Close registration (security) — COMPLETE by Watson 2026-07-02:** `POST /auth/register` is protected by server-local `AUTH_REGISTRATION_CODE` on Oracle. When the env var is set, register requires `registrationCode` in the request body; missing/wrong code returns `403` before account lookup. Existing accounts continue to log in normally. The web UI still has no register form (API-only).
  - Result: implemented env validation, register DTO, `AuthService.register` check, docs, and unit tests; local `format:check`, `lint`, `typecheck`, `test`, and `build` passed. Deployed API-only to Oracle as `localhost/psa-api:5e26bb1`; `psa-web` remains `localhost/psa-web:4d4766d`; `psa-db` was not restarted. Runtime env was generated from the live API container so Podman gets expanded secrets; the registration code value is server-local and was not printed or committed. Production smoke: register without code `403`, wrong code `403`, correct code `201`, login `200`, temporary partnership/session created, new session instantiated 30 clauses from `mvp-draft-2`, temporary data cleaned up. Public SPA, `/api/health`, and `/api/health/db` return `200`.
- [ ] **W3 Password change — PAUSED pending D1 by owner direction 2026-07-03:** self-service `POST /auth/change-password` (current password + new, argon2, throttled) + admin reset in the `accounts` module (set a temporary password). Follow the existing auth-module patterns (zod, guards, tests).
  - State: implementation was started and committed as `6a529b4` (`feat: add password change flows`), but W3 is **not complete and not deployed**. Do not continue, polish, deploy, or mark W3 complete until D1 lands, unless the owner explicitly changes the order. The reason is plan control: D1 owns the session-day/topbar/navigation pass, and W3 UI should follow it rather than surprise the D1 work. No Prisma migration, no new env, no seed.
- [ ] **W4 Committed e2e suite:** turn the per-step smoke scripts (see completed-log entries for what they covered: auth/CSRF/isolation, partnerships CRUD, partners cap+reorder, session lifecycle, scenario instantiation+status rules, capture+signoffs+versions) into a committed `tests/e2e` suite runnable as `pnpm test:e2e` against a disposable compose db (migrate + seed + run API + assert). Document in README; run it before every deploy from now on.
- [ ] **W5 Server-side HTML sanitization (security, blocks D4):** `clause.text` stores TipTap HTML unsanitized. Sanitize on write in `ScenarioService.updateClause` with an allowlist matching StarterKit output (`p, br, strong, em, s, ul, ol, li, h1–h3, blockquote, code, pre`), e.g. via `sanitize-html`; strip everything else (attributes too). Unit-test with `<script>`/`onerror` payloads. Required before the agreement export (D4) renders this HTML into documents.
- [ ] **W6 Host health cron (low):** every ~5 min curl the public SPA + `/api/health` + `/api/health/db` from the Oracle host, log locally (journal/file, no external services — SEC-7); on repeated failure leave a clear journal marker. Optional: restart hint in the log message.
- Coordination (D↔W sync points; otherwise the two plans run independently in parallel):
  - **Hard: W5 before D4.** The agreement export (D4) renders clause HTML into PDF/DOCX — sanitization must be live first. Everything else in D1–D5 does not depend on W-items.
  - **Soft: W3 after D1, now owner-confirmed.** D1 rebuilds the topbar/навигацию; W3 has a started commit (`6a529b4`) but is paused and must not be continued/deployed until D1 lands, unless the owner explicitly changes the order.
  - **W2 side effect:** once the registration code is live, any smoke/e2e that registers accounts must pass `AUTH_REGISTRATION_CODE` (W4's suite reads it from env). The demo seed (D5) writes via Prisma, not the register API — unaffected.
  - **Operational:** one prod environment — deploys serialize through Watson regardless of which plan an item came from. Both agents commit to `main`: pull/rebase before push; HANDOFF is the hot file — keep edits small and section-scoped.
  - Recommended Watson order: **W1 → W2 → W5 → W4 → W3 (after D1) → W6**; W5 ideally done while Claude is on D2/D3 so D4 is never blocked.
  - Deploys of W-items follow the usual runbook (image rebuild + recreate; W2/W3/W5 are code-only, no migrations).

### Oracle Podman logging deploy note (COMPLETE)
- Owner: Watson — started: 2026-06-27; completed: 2026-06-27
- Goal: Record the future-deploy instruction for the known Oracle Podman
  `journald` noise issue without touching the live server or containers now.
- Result: Added an Oracle Podman logging note to `deploy/README.md`. Next
  controlled Podman recreate should move `psa-db`, `psa-api`, and `psa-web`
  from `journald` logging to a file-backed log driver such as
  `--log-driver=k8s-file --log-opt max-size=10mb`, preserving names, ports,
  volumes, restart policy, and Caddy routing. Verify `podman logs` still works,
  log size limits are in place, local and public health checks pass, HTTP
  redirects to HTTPS, and `journalctl -b -p err..alert` no longer contains
  normal startup/deprecation noise. Do not do a hot in-place logging tweak only
  for cosmetics; change it only as part of a controlled deploy/recreate with a
  rollback path.
- Next step: On the next Watson-led production deploy/recreate, include the log
  driver change in the Podman run parameters and perform the listed checks.

### Phase 1 — Cases, sessions, scenario, capture (COMPLETE)
- Owner: code-writing agent (Claude) — Plan committed: 2026-06-23
- Goal: first minimally useful product — partnership → partners → session (30 blocks instantiated) → scenario walk → capture agreements → partner sign-offs → version history. Full detail: `docs/plans/phase-1-cases-sessions-scenario-capture.md`.
- Plan (ordered, each step independently committable):
  - [x] 1.1 Partnerships CRUD — DONE: `partnerships` API module (create/list+search/filter/get/update/archive/restore/delete; owner isolation via `assertCanAccessOwned`, architect+admin roles); web list + create + detail pages behind an `AppLayout`; unit tests (service isolation + DTOs) + full-stack smoke (CRUD, cross-architect 403, CSRF). No new migration.
  - [x] 1.2 Partners — DONE: nested `partners` API (list/add/update/remove/reorder; **max 5** hard cap enforced on add; min 2 not forced — checked at session start in 1.3; owner-isolated through the parent partnership, architect+admin roles) + a partners editor on the partnership detail page (add, inline edit, ↑/↓ reorder, remove-with-confirm). Unit tests (cap, permutation reorder, isolation) + full-stack API smoke. No schema change. (FR-2.3, owner-overridden upper bound)
  - [x] 1.3 Sessions — DONE: nested `sessions` API (create initial/review with a mandatory, validated baseline for review; list/get; start [draft→in_progress, guarded by ≥2 partners] / complete [in_progress→completed]; delete; owner-isolated through the parent partnership) + a sessions panel on the partnership detail page (create with kind/baseline, status badges, start/complete/delete). Unit tests + full-stack API smoke (lifecycle, ≥2-partner guard, baseline rules, CSRF, isolation). No schema change. FR-2.5 completion warnings (heavy blocks/matrix/shares) and `next_review_at` (FR-9.1) deferred to 1.4/3/2/5. (FR-2.4–2.5)
  - [x] 1.4 Scenario engine — DONE: session creation instantiates the 30 blocks (`mvp-draft-1`) as `clause` rows atomically (fixed order, FR-3.1); nested `scenario` API (list clauses + question fields; PATCH status with the `agreed`-requires-text and `not_applicable`-requires-reason rules), owner-isolated through the session's partnership; a scenario page (block display, status control, «неактуально» confirm+reason, progress counters). Unit tests + full-stack smoke (instantiate 30, status rules, CSRF, isolation). No schema change / no re-seed. `agreed` stays gated until 1.5 adds text. (FR-3.1–3.6)
  - [x] 1.5 Capture (FR-4.1–4.5) — DONE and live:
    - [x] 1.5a DONE: clause PATCH (`updateClause`) accepts `text`/`rationale` (agreed-requires-text counts same-request text); scenario page formulation + rationale textareas with debounced autosave (≤2s); unlocks `agreed`. `source` stays `manual` (no UI; non-manual sources arrive with AI in Phase 7). Unit tests + full-stack smoke. No schema change. (FR-4.1, 4.2, 4.5)
    - [x] 1.5b DONE: `PUT …/clauses/:id/signoffs/:partnerId {agreed}` upserts a `clause_signoff` (set/clear `signedAt`); clause list includes sign-offs; scenario page per-partner «согласен» checkboxes + «подтверждено всеми» badge. Owner-isolated (clause∈session, partner∈partnership). Unit tests + full-stack smoke. No schema change. (FR-4.3)
    - [x] 1.5c DONE: `clause_version` snapshot on the `agreed` transition, on explicit «save version» (`POST …/versions {note?}`), and before a restore (`POST …/versions/:id/restore`, non-destructive); `GET …/versions` history. Scenario page: per-block history toggle + «сохранить версию» + «откатить». Owner-isolated. Unit tests + full-stack smoke. No schema change. (FR-4.4)
    - [x] 1.5d DONE: TipTap/ProseMirror rich formulation editor (`@tiptap/react` + `starter-kit` + `pm`) replacing the textarea; `clause.text` is editor HTML (empty → `''`); toolbar bold/italic/list; version history/rollback round-trip the HTML. Browser-verified (login → type + bold → autosave persisted as HTML) + frozen-lockfile install & vite build verified. No migration/seed/API change. (§7.2)
- How to resume: Phase 1 is complete and live. Development continues per the **demo-first re-plan** (owner decision 2026-07-02): next is step **D1** of `docs/plans/demo-readiness.md` (see the "Demo readiness" active task above); the Gritz Calculator (Phase 2) and matrix (Phase 3) come after the demo feedback. Watson works the W1–W6 ops/security backlog in parallel. Reuse the scenario/clause + `assertCanAccessOwned` patterns; web via React Router + TanStack Query + the `/api` client. Bring up the dev stack per `deploy/README.md` (or `pnpm --filter @psa/api dev` + `pnpm --filter @psa/web dev` with the db in Docker/Podman).

### Intermediate deploy — Phase 1.5c + 1.5d (version history + TipTap editor) to Oracle (COMPLETE)
- Owner: Watson — Handoff committed: 2026-07-01; deployed: 2026-07-01
- Goal: Roll Phase 1.5c (formulation version history + rollback) **and** 1.5d (TipTap rich formulation editor) onto the live Oracle host — one combined deploy off the latest `main` (both were built on top of the deployed 1.5b, neither deployed yet). This completes Phase 1.
- Source: the latest `main` (the 1.5d feature commit; includes 1.5c). Change surface:
  - **1.5c — application code only:** scenario version endpoints (`GET/POST …/clauses/:id/versions`, `POST …/versions/:versionId/restore`) + auto-snapshot on the `agreed` transition (`apps/api/src/scenario/`) and the scenario page history/rollback UI.
  - **1.5d — application code + a new front-end dependency:** the formulation editor is now TipTap. `apps/web/package.json` and **`pnpm-lock.yaml` change** (adds `@tiptap/react` + `@tiptap/starter-kit` + `@tiptap/pm`, and their transitive deps). No API change; `clause.text` now stores HTML.
  - **No Prisma schema change / no migration** — the `clause_version` (and all other) tables exist since step 0.4. **No re-seed. No new env.**
- Deploy note (the important one): the **web image build** (`apps/web/Dockerfile`) runs `pnpm install --frozen-lockfile --filter @psa/web...` then `vite build`. The committed lockfile is consistent with the new `@tiptap/*` deps (verified: the exact frozen-lockfile install **and** `vite build` both pass locally), so a normal `podman build -f apps/web/Dockerfile` picks up TipTap and succeeds — no special steps. The web bundle grows (~232 kB → ~544 kB / ~168 kB gzip) — expected for ProseMirror. The `api` image is unchanged in behaviour (rebuild is fine/optional).
- Plan: pull `main`, rebuild the `psa-api` + `psa-web` images (the web build installs the new deps from the frozen lockfile), recreate those two containers (`psa-db` untouched); **do not** run `db:seed` or any migration.
- Verify:
  - Regression: `https://partners-architector.duckdns.org/` → 200 (SPA), `/api/health` + `/api/health/db` → `{"status":"ok"}`, HTTP → 308.
  - 1.5c: in a session's scenario, «Сохранить версию» adds a version, «История версий» lists them, «Откатить» restores a past formulation (the current one is snapshotted first); marking a block «согласовано» auto-adds a version. `GET …/clauses/:id/versions` → 200 array.
  - 1.5d: the formulation field is now a rich editor with a bold/italic/list toolbar; typing autosaves and a reload keeps the formatting; the stored `clause.text` is HTML (e.g. `<p><strong>…</strong></p>`).
- Notes: same Podman/Quadlet runtime shape (ADR 0002); no CI/CD, deploy keys, server credentials, or secrets committed.
- Result: deployed application commit `4d4766d` as `/opt/partners-architector/releases/4d4766d`; `/opt/partners-architector/current` now points to it. Built `localhost/psa-api:4d4766d` and `localhost/psa-web:4d4766d`, preflighted the API candidate against the live database, then replaced only `psa-api` and `psa-web`; `psa-db`, `psa_pgdata`, schema, seed, and server-local env were untouched.
- Verification: local `pnpm install --frozen-lockfile`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm build` passed before the Oracle deploy. Public SPA `200`; `/api/health` and `/api/health/db` `200`; HTTP `308` to HTTPS. Oracle current release and images are `4d4766d`; `psa-db` retained its existing container. Full temporary production feature smoke passed and cleaned up: register/login architect, create partnership with two partners and an initial session, verify 30 clauses, save TipTap-style HTML (`<p><strong>Watson bold smoke</strong></p>`), create an explicit version, mark the clause `agreed` and see an auto-version, restore the explicit version, verify rollback restored the HTML and first snapshotted the current wording, then delete the smoke partnership. Caddy and the host system are healthy with no failed units.

### Intermediate deploy — Phase 1.5b (partner sign-offs) to Oracle (COMPLETE)
- Owner: Watson — Handoff committed: 2026-06-30; deployed: 2026-06-30
- Goal: Roll Phase 1.5b (per-partner sign-offs on scenario blocks) onto the live Oracle host.
- Source: the Phase 1.5b commit on `main` (latest). Change surface: **application code only** — a `PUT …/clauses/:clauseId/signoffs/:partnerId` endpoint and the clause list now includes each clause's sign-offs (`apps/api/src/scenario/`), plus the scenario page's per-partner checkboxes (`apps/web/src/pages/ScenarioPage.tsx` + `api/scenario.ts` + `partnerships/clauseHooks.ts`). Therefore:
  - **No Prisma schema change / no new migration** — the `clause_signoff` table exists since step 0.4.
  - **No re-seed, no new dependencies, no new env.**
- Plan: pull `main`, rebuild the `psa-api` + `psa-web` images, recreate those two containers (`psa-db` untouched); **do not** run `db:seed` or any migration.
- Verify:
  - Regression: `https://partners-architector.duckdns.org/` → 200 (SPA), `/api/health` + `/api/health/db` → `{"status":"ok"}`, HTTP → 308.
  - Feature: in a session's scenario, each block shows a «согласен» checkbox per partner; ticking all shows «✓ подтверждено всеми». API-level: `PUT …/clauses/:id/signoffs/:partnerId {"agreed":true}` → 200, and the clause list includes a `signoffs` array.
- Notes: same Podman/Quadlet runtime shape (ADR 0002); no CI/CD, deploy keys, server credentials, or secrets committed.
- Result: deployed application commit `36a9d02` as `/opt/partners-architector/releases/36a9d02`; `/opt/partners-architector/current` now points to it. Built `localhost/psa-api:36a9d02` and `localhost/psa-web:36a9d02`, preflighted the API candidate against the live database, then replaced only `psa-api` and `psa-web`; `psa-db`, `psa_pgdata`, schema, seed, and server-local env were untouched. API/Web continue to use Podman `k8s-file` logs; `psa-db` remains on `journald` until a future controlled DB recreate is otherwise needed.
- Verification: local checks `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm build` passed. Public SPA `200`; `/api/health` and `/api/health/db` `200`; HTTP `308` to HTTPS. Full temporary feature smoke passed and cleaned up: register/login architect, create partnership with two partners and an initial session, verify 30 clauses and a `signoffs` array, set two partner sign-offs to agreed (`signedAt` set), confirm the clause list returns two signed sign-offs, clear one sign-off (`signedAt: null`), then delete the smoke partnership. API startup logs show the sign-offs route and a successful Prisma connection. Caddy and `podman-restart.service` are active; systemd has no failed units; `podman logs` works for the file-backed API/Web logs; no new boot error journal entries appeared after the switch.

### Intermediate deploy — Phase 1.5a (capture text) to Oracle (COMPLETE)
- Owner: Watson — Handoff committed: 2026-06-30; deployed: 2026-06-30
- Goal: Roll Phase 1.5a (per-clause formulation text + rationale + autosave) onto the live Oracle host.
- Source: the Phase 1.5a commit on `main` (latest). Change surface: **application code only** — the `scenario` clause PATCH generalized to also accept `text`/`rationale` (`apps/api/src/scenario/`), and the scenario page's formulation + rationale textareas with debounced autosave (`apps/web/src/pages/ScenarioPage.tsx` + `api/scenario.ts` + `partnerships/clauseHooks.ts`). Therefore:
  - **No Prisma schema change / no new migration** — `clause.text` / `rationale` / `source` exist since step 0.4.
  - **No re-seed, no new dependencies, no new env** (TipTap is the later sub-step 1.5d).
- Plan: pull `main`, rebuild the `psa-api` + `psa-web` images, recreate those two containers (`psa-db` untouched); **do not** run `db:seed` or any migration.
- Verify:
  - Regression: `https://partners-architector.duckdns.org/` → 200 (SPA), `/api/health` + `/api/health/db` → `{"status":"ok"}`, HTTP → 308.
  - Feature: in a session's scenario, typing into a block's «Формулировка договорённости» autosaves (reload keeps it); once a block has text, its status can be set to «согласовано». API-level: `PATCH …/clauses/:id {"status":"agreed"}` → 409 with no text, 200 after text is saved. (The clauses route was already live since 1.4; 1.5a only widens its accepted body.)
- Notes: same Podman/Quadlet runtime shape (ADR 0002); no CI/CD, deploy keys, server credentials, or secrets committed.
- Result: deployed application commit `4bfc23b` as `/opt/partners-architector/releases/4bfc23b`; `/opt/partners-architector/current` now points to it. Built `localhost/psa-api:4bfc23b` and `localhost/psa-web:4bfc23b`, preflighted the API candidate against the live database, then replaced only `psa-api` and `psa-web`; `psa-db`, `psa_pgdata`, schema, and seed were untouched. The server-local `deploy/.env` remains server-only; it was copied into the new release path because GitHub release tarballs do not contain ignored secrets. During the recreate, `psa-api` and `psa-web` were moved from `journald` to `k8s-file` logs with a `10MB` size cap. `psa-db` intentionally remains on `journald` until a future controlled DB recreate is otherwise needed.
- Verification: public SPA `200`; `/api/health` and `/api/health/db` `200`; HTTP `308` to HTTPS. Full temporary smoke passed and cleaned up: register/login architect, create partnership/session, verify 30 clauses, `agreed` without text returns `409`, saving `text`/`rationale` returns `200`, setting `agreed` after text returns `200`, then delete the smoke partnership. API startup logs show the scenario PATCH route and a successful Prisma connection. Caddy and `podman-restart.service` are active; systemd has no failed units; `podman logs` works for the new file-backed API/Web logs.

### Intermediate deploy — Phase 1.4 (scenario engine) to Oracle (COMPLETE)
- Owner: Watson — Handoff committed: 2026-06-24; deployed: 2026-06-25
- Goal: Roll the Phase 1.4 scenario engine onto the live Oracle host.
- Source: the Phase 1.4 commit on `main` (latest). Change surface: **application code only** — a new `apps/api/src/scenario/` module (registered in `app.module.ts`), clause instantiation added to `SessionsService.create`, and the web scenario page + route (`apps/web/src/pages/ScenarioPage.tsx`, `api/scenario.ts`, `partnerships/clauseHooks.ts`). Therefore:
  - **No Prisma schema change / no new migration** — the `clause` table and `clause_status` / `clause_source` enums exist since step 0.4.
  - **No re-seed** — instantiation reads the existing seeded `question` rows (the 30 blocks loaded at the first Oracle deploy). No new env or dependencies.
- Plan:
  - [x] Pull `main`, build the `psa-api` + `psa-web` images, and recreate those two containers (`psa-db` untouched).
  - [x] Do not run `db:seed` or a manual migration; allow only the API entrypoint's idempotent migration check.
  - [x] Verify the public stack and guarded scenario route, then record the result.
- Verify:
  - Regression: `https://partners-architector.duckdns.org/` → 200 (SPA), `/api/health` + `/api/health/db` → `{"status":"ok"}`, HTTP → 308.
  - Feature is live (non-destructive): `GET /api/partnerships/<uuid>/sessions/<uuid>/clauses` **unauthenticated → 401** (route mounted; was 404 before 1.4). API startup logs should show `ScenarioModule`. Creating a new initial session in the UI should show 30 blocks.
- Caveat: instantiation runs **at session creation**, so any sessions created **before** this deploy (e.g. during 1.3 testing) have no clauses — create a fresh session to see the scenario. Sessions created after the deploy get the 30 blocks.
- Notes: same Podman/Quadlet runtime shape (ADR 0002); no CI/CD, deploy keys, server credentials, or secrets committed.
- Result: deployed application commit `fc06274` as `/opt/partners-architector/releases/fc06274`; `/opt/partners-architector/current` now points to it. Built `localhost/psa-api:fc06274` and `localhost/psa-web:fc06274`, then preflighted the API in an isolated candidate container against the live database before replacing API/web. Rollback to `8a9808d` was prepared but not needed. `psa-db` retained the same container ID and start time; `psa_pgdata`, schema, and seed were untouched. The entrypoint reported no pending migrations, and the existing `mvp-draft-1` question set still contains 30 rows. Removed only dangling build layers after verification; current and rollback images remain tagged.
- Verification: public SPA `200`; `/api/health` and `/api/health/db` `200`; unauthenticated nested clauses route `401`; HTTP `308` to HTTPS. API startup logs show `ScenarioModule`, both clauses routes, and a successful Prisma connection. Caddy and `podman-restart.service` are active; systemd has no failed units.

### Intermediate deploy — Phase 1.3 (sessions) to Oracle (COMPLETE)
- Owner: Watson — Handoff committed: 2026-06-24; deployed: 2026-06-24
- Goal: Roll the Phase 1.3 sessions feature onto the live Oracle host on top of the running stack.
- Source: the Phase 1.3 commit on `main` (latest). Change surface: **application code only** — a new `apps/api/src/sessions/` module (registered in `app.module.ts`) and the web sessions panel (`apps/web/src/components/SessionsSection.tsx` + `api/sessions.ts` + `partnerships/sessionHooks.ts`, wired into the partnership detail page). Therefore:
  - **No Prisma schema change / no new migration** — the `session` table and the `session_kind` / `session_status` enums exist since step 0.4. The api entrypoint's idempotent `migrate deploy` is a no-op.
  - **No new dependencies, env vars, or seed** — same `psa.env` contract; methodology blocks unchanged.
- Plan (identical shape to the 1.1/1.2 deploys):
  - [x] Pull `main` into the deployment checkout, rebuild the `psa-api` + `psa-web` images, and recreate those two containers (`psa-db` untouched).
  - [x] **Do not** run `db:seed` or any manual migration — nothing changed.
  - [x] Verify externally (below), then record the result here and commit.
- Verify:
  - Regression: `https://partners-architector.duckdns.org/` → 200 (SPA), `/api/health` and `/api/health/db` → `{"status":"ok"}`, HTTP → 308.
  - Feature is live (non-destructive): `GET /api/partnerships/<any-uuid>/sessions` **unauthenticated → 401** (the nested route now exists and is guarded; it was 404 before 1.3). API startup logs should show `SessionsModule` and the create/list/start/complete/delete routes.
- Notes: same Podman/Quadlet runtime shape (ADR 0002); no CI/CD, deploy keys, server credentials, or secrets committed — secrets stay only in the server-local `psa.env`.
- Result: deployed application commit `8a9808d` as `/opt/partners-architector/releases/8a9808d`; `/opt/partners-architector/current` now points to it. Built and started `localhost/psa-api:8a9808d` and `localhost/psa-web:8a9808d` using the existing runtime settings. The API first passed an isolated candidate-container health/readiness check against the live database; rollback to `70cdedf` was prepared but not needed. `psa-db` and `psa_pgdata` were not restarted or modified. No seed or manual migration was run; the entrypoint migration check had nothing new to apply. Removed only dangling intermediate Podman build layers after verification, reducing root filesystem usage from 60% during the build to 50%; tagged current and rollback images were retained.
- Verification: public SPA `200` from `158.101.210.207`; `/api/health` `200`; `/api/health/db` `200`; unauthenticated nested sessions route `401`; HTTP `308` to HTTPS. API startup logs show `SessionsModule` and the list/create/get/start/complete/delete routes. Caddy and `podman-restart.service` are active; systemd is running with no failed units.

### Intermediate deploy — Phase 1.2 (partners) to Oracle (COMPLETE)
- Owner: Watson — Deployed: 2026-06-24
- Source: application commit `70cdedf`; application code only, with no Prisma schema/migration, dependency, env, or seed changes.
- Result: deployed as `/opt/partners-architector/releases/70cdedf`; `/opt/partners-architector/current` now points to it. Built and started `localhost/psa-api:70cdedf` and `localhost/psa-web:70cdedf` with the existing network, loopback ports, restart policy, and server-local secrets. The API image first passed an isolated candidate-container health/readiness check against the live database. Rollback to `389a934` was prepared but not needed. `psa-db` and `psa_pgdata` were not restarted or modified; no seed was run, and the entrypoint's idempotent `migrate deploy` had no new migration to apply.
- Verification: public SPA `200` from `158.101.210.207`; `/api/health` `200`; `/api/health/db` `200`; unauthenticated nested partners route `401` (route exists and is guarded); HTTP `308` to HTTPS. API startup logs show `PartnersModule` and all list/add/reorder/update/delete routes. Caddy and `podman-restart.service` are active; systemd is running with no failed units.

### Intermediate deploy — Phase 1.1 (partnerships) to Oracle (COMPLETE)
- Owner: Watson — Handoff committed: 2026-06-24; deployed: 2026-06-24
- Goal: Roll the Phase 1.1 partnerships feature onto the live Oracle host (`https://partners-architector.duckdns.org/`) on top of the running Phase 0 stack.
- Source: commit `389a934` on `main`. Change surface (verified by `git show --stat`): **application code only** — a new `apps/api/src/partnerships/` API module (registered in `app.module.ts`) and the web list/detail pages (`apps/web/src/…`). What this means for the deploy:
  - **No Prisma schema change and no new migration** — reuses the `partnership` table from step 0.4. The api auto-runs `migrate deploy` on start; here it is a no-op.
  - **No new dependencies** — `package.json` and `pnpm-lock.yaml` are unchanged.
  - **No new env vars / no config change** — same `psa.env` contract as the first deploy.
  - **No re-seed** — methodology blocks / settings are unchanged.
- Plan:
  - [x] Pull `main` (`389a934`) into the deployment checkout (`/opt/partners-architector/current`).
  - [x] Rebuild both images (build context = repo root):
    `podman build -f apps/api/Dockerfile -t localhost/psa-api:latest .` and `podman build -f apps/web/Dockerfile -t localhost/psa-web:latest .`
  - [x] Recreate the api + web containers from the new images (`psa-db` is untouched):
    - current live form (manual rootful containers + `podman-restart.service`): `podman stop psa-api psa-web && podman rm psa-api psa-web`, then re-run both with the **same flags as the first deploy** (same `psa` network, `--env-file` pointing at the server-local `psa.env`, web published on `127.0.0.1:8080`);
    - or, if migrated to Quadlet: `systemctl restart psa-api psa-web`.
  - [x] **Do not** run `db:seed` or any manual migration — neither changed. The API entrypoint's idempotent `migrate deploy` ran on startup and found no new migration.
  - [x] Verify externally (below), then record the result here and commit.
- Verify:
  - Regression (stack healthy after rebuild): `https://partners-architector.duckdns.org/` → 200 (SPA), `/api/health` and `/api/health/db` → `{"status":"ok"}`, HTTP → 308.
  - Feature is live (non-destructive): `GET /api/partnerships` **unauthenticated → 401** (the route now exists and is guarded; it was 404 before 1.1). Optionally, log in as an architect and confirm the partnerships list loads and "create" works in the UI.
- Notes: keep the Podman target (`deploy/podman/`) and `deploy/docker-compose.yml` as the reference runtime shape (ADR 0002). No CI/CD, deploy keys, server credentials, or secrets are committed — secrets stay only in the server-local `psa.env` (mode `600`).
- Result: deployed commit `389a934` as release `/opt/partners-architector/releases/389a934`; `/opt/partners-architector/current` now points to it. Built and started `localhost/psa-api:389a934` and `localhost/psa-web:389a934` with the existing network, loopback ports, restart policy, and server-local secrets. The new API image first passed a separate candidate-container health/readiness check against the live database; rollback to the previous `3acaec9` images was prepared for the switch but was not needed. `psa-db` and `psa_pgdata` were not restarted or modified.
- Verification: public SPA `200` from `158.101.210.207`; `/api/health` `200`; `/api/health/db` `200`; unauthenticated `/api/partnerships` `401` (route exists and is guarded); plain HTTP `308` to HTTPS. API startup logs show `PartnershipsModule`, all CRUD/archive/restore routes, and a successful Prisma database connection. Caddy and `podman-restart.service` are active; systemd is running with no failed units.
### First test deploy to Oracle (COMPLETE)
- Owner: Watson — Plan committed: 2026-06-23; started: 2026-06-23
- Goal: Manually deploy the completed Phase 0 stack to the Oracle production host behind Caddy at `https://partners-architector.duckdns.org/`, replacing the static placeholder with the web/API/PostgreSQL runtime stack.
- Plan:
  - [x] Pull the current `main` on the Oracle host into the existing deployment checkout or create one if missing.
  - [x] Create server-local `deploy/.env` with generated production secrets; keep it untracked.
  - [x] Build and start `db`, `api`, and `web` using the runtime shape defined by `deploy/docker-compose.yml`.
  - [x] Run the idempotent seed command so the 30 methodology blocks and legal presets exist.
  - [x] Repoint Caddy from the placeholder root to the web container and keep HTTPS enabled.
  - [x] Verify externally: HTTPS 200 for the SPA, `/api/health` 200, `/api/health/db` 200, and HTTP-to-HTTPS redirect.
  - [x] Record the deploy result here and commit it.
- Result: Live on Oracle (`158.101.210.207`) from commit `3acaec9`. Runtime files are under `/opt/partners-architector/current`; generated secrets are only in server-local `deploy/.env` (mode `600`). Oracle did not have Docker/Compose installed, so the first deploy used an equivalent manual rootful Podman runtime: `psa-db` (PostgreSQL/pgvector, named volume `psa_pgdata`), `psa-api` (localhost `3001`), and `psa-web` (localhost `8080`). Caddy reverse-proxies `partners-architector.duckdns.org` to `127.0.0.1:8080`; `podman-restart.service` is enabled so restart-policy containers are restored after reboot. No CI/CD, deploy keys, server credentials, or generated secrets were committed.
- Verification: external `https://partners-architector.duckdns.org/` returned `200` from `158.101.210.207` with title `Помощник партнёрских сессий`; `/api/health` returned `200` with `{"status":"ok"}`; `/api/health/db` returned `200` with `{"status":"ok"}`; plain HTTP returned `308` to HTTPS. The seed command reported `Seed complete: 30 questions; settings created.`
- Next step: Start Phase 1 (cases, sessions, scenario engine, capture) on top of the deployed Phase 0 skeleton.

### Phase 0 — Skeleton & infrastructure (COMPLETE)
- Owner: code-writing agent (Claude) — Plan committed: 2026-06-22; started: 2026-06-22
- Goal: Establish the monorepo, dev infra (Docker Compose + PostgreSQL/pgvector), Prisma schema + migrations, base auth/RBAC (3 roles), and seed data (30 questions, legal carriers) — a running skeleton later phases build on. Full detail: `docs/plans/phase-0-skeleton-and-infrastructure.md`.
- Plan (ordered, each step independently committable):
  - [x] 0.1 Monorepo & tooling baseline — DONE: pnpm workspaces (`apps/*`, `packages/*`), TS strict (`tsconfig.base.json`), ESLint 9 + Prettier, Vitest, root scripts, dir skeleton, commands documented in CLAUDE.md/README
  - [x] 0.2 Dev infra — DONE: `deploy/docker-compose.yml` (`db` = postgres16 + pgvector, healthcheck, named volume; api/web commented for 0.3/0.7), `deploy/.env.example`, `deploy/db/init` enables pgvector. Live-verified.
  - [x] 0.3 API skeleton (NestJS) — DONE: ConfigModule + zod env validation, `/health` (200) + `/health/db` (200/503), PrismaModule (minimal schema in `apps/api/prisma`), global exception filter, `apps/api/Dockerfile`, compose `api` wired. Live-verified (Node + container). Request-validation pipe deferred to 0.6 (zod-based).
  - [x] 0.4 Prisma schema + initial migration — DONE: 19 core entities + enums (§5) in `apps/api/prisma/schema.prisma`, migration `20260622120000_init_core_schema` (pgvector extension, matrix-level CHECK); AI/ASR tables deferred to 7/8. Verified on a fresh DB + integration test.
  - [x] 0.5 Seed — DONE: 30 questions (App. A order + App. F texts) + carrier presets (App. B) + settings singleton in `apps/api/src/seed`; idempotent `db:seed` (tsx); pure + integration tests.
  - [x] 0.6 Auth & RBAC — DONE: register/login/logout/me (argon2, JWT httpOnly cookie), double-submit CSRF guard, throttler, RolesGuard + @Roles, JwtAuthGuard + @Public, ownership primitive (SEC-5), admin bootstrap from env, admin accounts module (FR-1.5); zod request pipe. Unit tests + live e2e verified.
  - [x] 0.7 Web skeleton — DONE: React 18 + Vite + TS, React Router, TanStack Query, API client (`/api` proxy + CSRF header), `LoginPage` + `ProtectedRoute` + Partnerships placeholder, ru i18n dictionary; pure unit tests; build + Vite `/api` proxy→API path verified live (browser click-through deferred to 0.8).
  - [x] 0.8 Wire-up + Phase 0 DoD — DONE: web prod image (nginx serves the SPA + proxies `/api`) + compose `web`; api auto-migrates on start (entrypoint) + resilient admin bootstrap; root `dev`; docs (deploy runbook, README, CLAUDE). Full stack verified end-to-end through nginx.
- Notes / decisions: Stack per ADR 0001. Implementation-level choices: pnpm workspaces, Vitest. Build in THIS repo (product codename `psa`; repo `partners-architector`). Deployment stays manual (Watson); no CI/CD or secrets committed.
- How to resume: Phase 0 is done. Next is the **first test deploy** — hand the `deploy/README.md` runbook to Watson (set `deploy/.env` secrets on the target, `up -d --build`, `db:seed`; add a TLS proxy + `AUTH_COOKIE_SECURE=true` for production). Then start **Phase 1** (cases, sessions, scenario engine, capture — spec §4.2–4.4): instantiate the 30 `question` blocks into a `session` as `clause` rows; block statuses / heavy flags / «неактуально»; live-protocol fields + autosave; partner sign-offs + `clause_version` history. Reuse the auth guards, the `ownership` isolation primitive, and the `@psa/api` feature-module pattern. A browser (Playwright) click-through of the SPA is still worth adding.
- Running the API/DB: `docker compose --env-file deploy/.env -f deploy/docker-compose.yml up -d db` (note: `deploy/.env` must include `AUTH_JWT_SECRET` even for db-only, because compose interpolates the `api` service), `pnpm --filter @psa/api db:deploy`, `pnpm --filter @psa/api db:seed`; run the API with `AUTH_JWT_SECRET` set. Author migrations via `prisma migrate diff` (avoid `prisma migrate reset` — gated by a dangerous-action guard). Start the Docker daemon first in this ephemeral env (`dockerd &`).
- Notes for this ephemeral env: start the Docker daemon first (`dockerd &`). The API image builds on a normal network as-is; inside this sandbox's TLS-intercepting proxy a build needs the proxy CA passed via `NODE_EXTRA_CA_CERTS` (do not commit that — it is environment-specific). Set `PRISMA_GENERATE_SKIP_AUTOINSTALL=true` is already baked into the api scripts/Dockerfile.

## Completed log

Most recent first.

- **2026-07-02 — Deployed W2 registration gate + O1 `mvp-draft-2` seed to Oracle.** Added `AUTH_REGISTRATION_CODE` support to the API (`registerSchema`, env validation, `AuthService.register`) with unit coverage and docs; local `format:check`, `lint`, `typecheck`, `test`, and `build` passed. Deployed API-only from commit `5e26bb1`: built `localhost/psa-api:5e26bb1`, preflighted a candidate against the live DB, then replaced `psa-api` while keeping `psa-db` and `psa_pgdata` untouched. `psa-web` remains image `4d4766d`; it was restarted once after API replacement so nginx re-resolved the `api` network alias. Set a server-local registration code without printing or committing it. Ran the O1 one-time seed through the new API container; DB now has `mvp-draft-1|30` and `mvp-draft-2|30`. Removed the temporary methodology PDF from the web container. Verification: public SPA/health/db-health `200`; registration without code `403`, wrong code `403`, correct code `201`; login/csrf `200`; temporary partnership/session smoke created 30 new clauses with the `mvp-draft-2` first prompt and was cleaned up. — Watson

- **2026-07-02 — O1: question texts revised against the methodology workbook (`mvp-draft-2`).** Rewrote all 30 seed blocks (`apps/api/src/seed/questions.ts`) against the owner-provided «Шаблон партнёрского соглашения» (Дмитрий Гриц): canonical titles (fuller №8/№14/№16/№19/№20/№21/№22/№25/№26/№27/№30, parentheticals dropped from №6/№7), prompts taken from the workbook's imperative instructions, helper questions from its guiding bullets. Key semantic fixes: №2 is «видение + миссия компании» (not "personal why"); №5 now carries the workbook's caveat that the Gritz Calculator is contraindicated for redistributing existing shares; №15 (not №16) hosts the authority matrix in the workbook — flagged for Phase 3. Structure (order/contours/heavy) unchanged per spec Appendix A; `QUESTION_SET_VERSION` bumped to `mvp-draft-2` so existing sessions keep their wording. The workbook PDF was fetched to the session scratchpad only (proprietary; not committed). Verified: lint/format/typecheck/build/test green (structural seed tests confirm order/heavy/contours intact) + live seed on a disposable DB (idempotent; 30 rows / 10 heavy under `mvp-draft-2`). Deploy = rebuild `psa-api` + one-time `db:seed` (handed to Watson). — code-writing agent (Claude)

- **2026-07-01 — Deployed Phase 1.5c + 1.5d to Oracle; Phase 1 live-complete.** Built API/web images from commit `4d4766d`, preflighted the API candidate against the live database, then replaced only `psa-api` and `psa-web`; `psa-db`, `psa_pgdata`, schema, seed, and server-local env were untouched. External checks passed: SPA/health/db-health `200`, HTTP→HTTPS `308`. Full temporary production smoke passed and was cleaned up: 30 clauses instantiated, TipTap-style HTML persisted, explicit version created, `agreed` created an auto-version, rollback restored the previous HTML and first snapshotted the current wording. Oracle current release and active API/Web images are `4d4766d`; systemd has no failed units. — Watson

- **2026-07-01 — Phase 1, step 1.5d (TipTap rich formulation editor). Phase 1 code-complete.** Replaced the formulation `textarea` on the scenario page with a TipTap/ProseMirror editor (spec §7.2): a new `RichTextEditor` component (`@tiptap/react` + `@tiptap/starter-kit` + `@tiptap/pm`, added to `@psa/web`) with a bold/italic/bullet-list toolbar. `clause.text` now stores the editor's HTML; an empty document is normalized to `''` so the API's `agreed`-requires-text guard still holds. External value changes (version rollback) sync into the editor via `setContent` without echoing back through onUpdate; the autosave contract (`updateClause {text}`, ≤2s debounce) is unchanged, and version history/rollback round-trip the HTML (previews strip tags). No API/schema change. Verified: lint/format/typecheck/build green (99 tests); the web-image commands `pnpm install --frozen-lockfile --filter @psa/web...` + `vite build` both pass (deploy de-risk); and a **real-browser smoke** (playwright-core + the pre-installed Chromium against `vite dev` + the API) — logged in via the UI, typed into the editor, applied bold via the toolbar, saw «Сохранено», and confirmed the clause autosaved as `<p><strong>Согласие сторон о тесте</strong></p>`. This finishes step 1.5 and all of Phase 1 (pending the combined 1.5c + 1.5d deploy). — code-writing agent (Claude)
- **2026-06-30 — Phase 1, step 1.5c (formulation version history + rollback).** Added `clause_version` history to the `scenario` module (FR-4.4): `GET …/clauses/:id/versions` (newest first), `POST …/versions {note?}` (explicit snapshot of the current formulation), and `POST …/versions/:versionId/restore` (re-applies a past version's text/rationale/status to the clause). A version is snapshotted before significant events — automatically on the transition to `agreed` (in `updateClause`, in a transaction), on an explicit «save version», and before a restore (so a rollback never loses the current wording and can itself be undone). Autosave keystrokes do **not** snapshot. Owner-isolated through the session's partnership (SEC-5). Web: each scenario block gained a «История версий» toggle, a «Сохранить версию» button, and per-version «Откатить» (with confirm); restore resets the editor's local state to the restored text. Tests: service (auto-snapshot on agree / not when already agreed, saveVersion note, restore snapshots-then-applies, version-in-clause guard) + DTO (unit), and a full-stack smoke (agree auto-snapshot → explicit save with note → autosave-no-snapshot → non-destructive rollback → 404/403 guards). No schema change — `clause_version` exists since 0.4. lint/format/typecheck/build/test green. — code-writing agent (Claude)
- **2026-06-30 — Phase 1, step 1.5b (partner sign-offs).** Added `PUT /partnerships/:id/sessions/:sid/clauses/:clauseId/signoffs/:partnerId {agreed}` to the `scenario` module: it upserts a `clause_signoff` (unique per clause+partner) and sets/clears `signedAt` with the `agreed` flag (FR-4.3); the clause is validated to belong to the session and the partner to the partnership, owner-isolated (SEC-5). The clause list (`listClauses`) now includes each clause's `signoffs`. Web: the scenario page shows a per-partner «согласен» checkbox on every block and a «✓ подтверждено всеми» badge once all partners agree. Tests: service (upsert set/clear timestamp, partner-in-partnership and clause-in-session guards) + DTO (boolean required) (unit), and a full-stack smoke (P1/P2 agree → fully confirmed; un-agree clears the timestamp; unknown partner/clause 404; bad body 400; CSRF 403; cross-architect 403). No schema change — `clause_signoff` exists since 0.4. lint/format/typecheck/build/test green. — code-writing agent (Claude)
- **2026-06-30 — Deployed Phase 1.5a capture text to Oracle.** Built API/web images from commit `4bfc23b`, preflighted the API candidate against the live database, then replaced only `psa-api` and `psa-web`; DB, volume, schema, and seed were untouched. API/Web now use Podman `k8s-file` logs with a `10MB` size cap; `psa-db` remains on `journald` until a future controlled DB recreate is otherwise needed. External checks passed: SPA/health/db-health `200`, HTTP→HTTPS `308`. Full temporary feature smoke passed and was cleaned up: 30 clauses instantiated, `agreed` before text returned `409`, text/rationale save returned `200`, and `agreed` after text returned `200`. — Watson

- **2026-06-30 — Phase 1, step 1.5a (capture: formulation text + autosave).** Generalized the `scenario` clause PATCH to a single `updateClause` that accepts `text` and `rationale` alongside `status`/`naReason` (the `agreed`-requires-text guard now counts text set in the same request); `source` stays `manual` until AI drafting (Phase 7). Web: the scenario page gained per-block «формулировка договорённости» + «мотивация/пояснение» textareas with debounced autosave (≤2s, FR-4.1) and a saving/saved indicator; the `agreed` status option enables once a block has text, and selecting it sends the current text so the guard passes — unlocking `agreed` end-to-end. Tests: service (text/rationale save, agreed-with-same-request-text) + DTO (text-only accepted, empty rejected) (unit), and a full-stack smoke (agreed-without-text 409 → save text 200 → agreed 200 → one-shot agreed 200 → rationale 200 → empty 400 → CSRF 403 → cross-architect 403). No schema change — `clause` fields exist since 0.4, so the intermediate deploy is an image rebuild + restart. lint/format/typecheck/build/test green. — code-writing agent (Claude)

- **2026-06-27 — Recorded Oracle Podman logging future-deploy note.** Captured
  the instruction for the next controlled Watson deploy/recreate: avoid the
  Podman `journald` log driver for `psa-db`, `psa-api`, and `psa-web` because
  `conmon` maps container stderr to journal priority 3, which makes normal
  PostgreSQL/nginx/Prisma startup or deprecation output appear as
  `journalctl -p err..alert` errors. Prefer a file-backed driver such as
  `k8s-file` with a size limit, keep `podman logs` usable, preserve container
  names/ports/volumes/restart policy/Caddy routing, and verify health plus
  journal cleanliness after recreate. No server or live container changes were
  made. — Watson
- **2026-06-25 — Deployed Phase 1.4 scenario engine to Oracle.** Built API/web images from commit `fc06274`, preflighted the API against the live database in an isolated candidate container, then replaced only `psa-api` and `psa-web`; DB, volume, schema, and seed were untouched. External checks passed: SPA/health/db-health `200`, guarded nested clauses route `401` unauthenticated, HTTP→HTTPS `308`; `ScenarioModule`, both routes, Caddy, restart service, and systemd are healthy. Pruned only dangling build layers while retaining current and rollback images. — Watson
- **2026-06-24 — Phase 1, step 1.4 (scenario engine).** Session creation now instantiates the 30 methodology blocks (question-set `mvp-draft-1`) as `clause` rows atomically, in fixed order (FR-3.1). Nested `scenario` API module (`@psa/api`): list a session's clauses with their question fields (number/title/prompt/helpers/category/heavy flag, ordered by `orderIndex`), and PATCH a clause's status with the FR-3.4 rule (`agreed` only when agreement text exists) and the FR-3.6 rule (`not_applicable` requires a reason; the reason is kept only while not-applicable) — owner-isolated through the session's partnership (`assertCanAccessOwned`, SEC-5). Web: a scenario page (`/partnerships/:id/sessions/:sid`, linked from the sessions panel) showing every block with category + heavy badges, a status selector (with the «неактуально» confirm + mandatory-reason dialog, and `agreed` disabled until there is text), and progress counters (agreed N of M, unclosed heavy, disputed/parked). Tests: service (ownership, agreed/na rules) + DTO validation (unit), and a full-stack smoke against Postgres (instantiate 30 ordered/not_started, status rules incl. 409/400/404, CSRF 403, cross-architect 403). No schema change and no re-seed — the `clause` table + enums exist since 0.4 and the questions are already seeded. Text capture (FR-4.x, which unlocks `agreed`) is step 1.5. lint/format/typecheck/build/test green. — code-writing agent (Claude)
- **2026-06-24 — Deployed Phase 1.3 sessions to Oracle.** Built API/web images from commit `8a9808d`, preflighted the API against the live database in an isolated candidate container, then replaced only `psa-api` and `psa-web`; DB, volume, schema, and seed were untouched. External checks passed: SPA/health/db-health `200`, guarded nested sessions route `401` unauthenticated, HTTP→HTTPS `308`; Caddy/systemd/restart service healthy. Pruned only dangling build layers after verification. — Watson
- **2026-06-24 — Phase 1, step 1.3 (sessions).** Nested `sessions` API module (`@psa/api`): create (initial, or review with a mandatory baseline validated to belong to the same partnership), list, get, `start` (draft→in_progress, guarded by the deferred ≥2-partners rule), `complete` (in_progress→completed), delete — all owner-isolated through the parent partnership (`assertCanAccessOwned`, SEC-5), `@Roles(architect, admin)`, zod DTOs. Web: a sessions panel on the partnership detail page — create (kind selector, baseline picker for review, optional title), status badges, and start/complete/delete actions (start disabled until ≥2 partners). Tests: service (lifecycle guards, ≥2-partner start, baseline rules, ownership) + DTO validation (unit), and a full-stack API smoke against Postgres (create→start-0-partners 409→add 2→start 200→complete 200→complete-again 409→review baseline 400/400/201→CSRF 403→cross-architect 403→delete 204). FR-2.5 completion warnings (heavy blocks/matrix/shares) and `next_review_at` (FR-9.1) deferred to the phases that add those (1.4/3/2/5). No schema change — `session` table + enums exist since 0.4, so the intermediate deploy is an image rebuild + restart. lint/format/typecheck/build/test green. — code-writing agent (Claude)
- **2026-06-24 — Deployed Phase 1.2 partners editor to Oracle.** Built API/web images from commit `70cdedf`, preflighted the new API against the live database in an isolated candidate container, then replaced only `psa-api` and `psa-web`; `psa-db`, its volume, schema, and seed were untouched. External checks passed: SPA/health/db-health `200`, guarded nested partners route `401` unauthenticated, HTTP→HTTPS `308`; Caddy/systemd/restart service healthy. — Watson
- **2026-06-24 — Phase 1, step 1.2 (partners).** Nested `partners` API module (`@psa/api`): list / add / update / remove / reorder, all scoped through the parent partnership's owner (`assertCanAccessOwned`, SEC-5), `@Roles(architect, admin)`, zod DTOs. **Max 5 partners** is a hard cap enforced on add (owner override of FR-2.3's "2–4"); the **min 2 is not enforced** while editing — it is validated later, at session start (1.3). Reorder takes the full ordered id list (validated as a permutation, no duplicates) and re-packs `order_index`; add appends after the current max so gaps from deletes never collide. Web: a partners editor on the partnership detail page — list, add form (disabled at 5, min-2 hint), inline edit, ↑/↓ reorder, remove-with-confirm; TanStack Query hooks + the `/api` client. Tests: service (cap, permutation/duplicate rejection, ownership through parent) + DTO validation (unit), and a full-stack API smoke against Postgres (add 5 / 6th→409 / list / reorder applied / CSRF-less→403 / cross-architect→403 / update→200 / delete-all→0). No schema change — the `partner` table exists since 0.4, so the intermediate deploy is an image rebuild + restart. lint/format/typecheck/build/test green. — code-writing agent (Claude)
- **2026-06-24 — Deployed Phase 1.1 partnerships CRUD to Oracle.** Built API/web images from commit `389a934`, preflighted the new API against the live database in an isolated candidate container, then replaced only `psa-api` and `psa-web` while leaving `psa-db`, its volume, schema, and seed untouched. External checks passed: SPA/health/db-health `200`, guarded partnerships route `401` unauthenticated, HTTP→HTTPS `308`; Caddy/systemd/restart service healthy. — Watson
- **2026-06-24 — Phase 1, step 1.1 (partnerships CRUD).** API `partnerships` module (`@psa/api`): create / list (search by name + status filter active|archived|all) / get / update / archive / restore / delete, owner-isolated via `assertCanAccessOwned` (architect sees own, admin all), `@Roles(architect, admin)`, zod DTOs. Web: `AppLayout` (topbar + logout via an Outlet layout route), a partnerships list with search/filter + create form, and a detail page (edit name/notes, archive/restore, delete with confirm), wired with TanStack Query hooks + the `/api` client. Tests: service ownership/scoping + DTO validation (unit) and a full-stack smoke through nginx (CRUD, cross-architect 403, CSRF-gated mutations, 404 after delete). No schema change — uses the existing `partnership` table, so an intermediate deploy is just an image rebuild + restart. lint/format/typecheck/build/test green. — code-writing agent (Claude)
- **2026-06-23 — Recorded dual deploy targets + prepared Phase 1.** Added ADR `docs/decisions/0002-dual-deploy-targets-docker-and-podman.md` and the Podman/Quadlet target under `deploy/podman/` (`psa.network` + `psa-db`/`psa-api`/`psa-web` `.container` units + README), mirroring `deploy/docker-compose.yml` (same images and env). Updated `deploy/README.md` to document both variants. Wrote the Phase 1 plan (`docs/plans/phase-1-cases-sessions-scenario-capture.md`) and set Phase 1 as the active (not-started) task. No feature code yet. — code-writing agent (Claude)
- **2026-06-23 — First Oracle test deploy.** Watson deployed the completed Phase 0 stack to Oracle behind Caddy at `https://partners-architector.duckdns.org/`. Because Oracle had no Docker/Compose, installed Podman from the standard Oracle Linux repository and ran the same runtime shape manually as rootful containers: `psa-db`, `psa-api`, and `psa-web` on a private Podman network, with Caddy reverse-proxying to the web container on `127.0.0.1:8080`. Generated production secrets stayed only in server-local `deploy/.env`; no secrets were committed. API migrations ran on container start; seed loaded 30 questions and settings. Verified externally: SPA `200`, `/api/health` `200`, `/api/health/db` `200`, HTTP `308` to HTTPS, and `podman-restart.service` enabled for reboot recovery. — Watson
- **2026-06-22 — Phase 0, step 0.8 (wire-up + Phase 0 DoD). Phase 0 complete.** Added the web production image (`apps/web/Dockerfile`, multi-stage: build SPA → nginx serving static + proxying `/api` to the api, with SPA history fallback; `apps/web/nginx.conf`) and activated the `web` service in `deploy/docker-compose.yml`. The api auto-applies migrations on container start (`apps/api/docker-entrypoint.sh`) and `AdminBootstrapService` is now resilient to an unmigrated DB (fixed a boot-crash). Added a root `dev` (parallel api+web watch), the deploy runbook (`deploy/README.md`), and README/CLAUDE getting-started. Verified end-to-end: built the api+web images and ran db+api+web together — SPA served (incl. deep route), `/api/health` + `/api/health/db` 200, register→login→me (architect), admin login + `/accounts` 200, architect `/accounts` 403, CSRF-gated logout, post-logout 401; seed loaded 30 blocks. lint/format/typecheck/build/test green. — code-writing agent (Claude)
- **2026-06-22 — Phase 0, step 0.7 (web skeleton).** Built the `@psa/web` SPA: React 18 + Vite + TypeScript, React Router (login + protected `/` Partnerships placeholder via `ProtectedRoute`), TanStack Query data layer, a fetch API client (`/api` base, `credentials: 'include'`, double-submit CSRF header on mutations), `useMe`/`useLogin`/`useLogout` hooks, and a ru i18n dictionary (NFR-4). Vite dev proxies `/api`→:3000 so the browser is same-origin (cookies/CSRF without CORS), mirroring the prod reverse proxy. Minimal CSS; the Radix/shadcn design system is deferred to feature phases. Pure unit tests (`readCookie`, i18n). Verified: lint/format/typecheck/build green (Vite build emits `dist/`), and live — Vite serves the SPA and the `/api` proxy reaches the API (`/api/health` 200, `/api/auth/me` 401); browser click-through deferred to 0.8. — code-writing agent (Claude)
- **2026-06-22 — Phase 0, step 0.6 (auth & RBAC).** Added the `auth` module: self-service architect registration + login/logout/me, argon2 hashing (`@node-rs/argon2`), JWT in an httpOnly `psa_session` cookie (account re-loaded per request so blocked accounts lose access immediately), double-submit CSRF (`psa_csrf` cookie + `x-csrf-token` header) guard, `@nestjs/throttler` rate limiting (5/min on login/register), global `JwtAuthGuard` (+`@Public`), `RolesGuard` (+`@Roles`), a `ZodBody` validation pipe (zod, no class-validator), an `AdminBootstrapService` (creates an admin from `AUTH_ADMIN_EMAIL`/`AUTH_ADMIN_PASSWORD` on first boot), and the `ownership` data-isolation primitive (SEC-5). Added an admin-only `accounts` module (list architects, block/activate — FR-1.5). Env gained `AUTH_*`; compose `api` passes them; `.env.example` updated; vitest configured for decorators. Verified: lint/format/typecheck/build + 33 unit tests green, and a full live e2e (register→login→me→RBAC 403→admin 200→wrong-pass 401→CSRF-gated logout→rate-limit 429). — code-writing agent (Claude)
- **2026-06-22 — Phase 0, step 0.5 (seed data).** Added the 30 methodology blocks (spec Appendix A order/category/heavy flag + Appendix F draft texts) as `apps/api/src/seed/questions.ts` (`QUESTION_SET_VERSION = mvp-draft-1`), the default carrier presets from Appendix B (`legal-presets.ts`), an idempotent `seed(prisma)` (upserts questions keyed by `(question_set_version, number)`, ensures one `settings` row) with a `db:seed` CLI (tsx) and `prisma.seed` config. Seed data lives under `src/seed` so feature phases can reuse it at runtime; removed the orphaned root `seed/`. Tests: pure data checks (30 blocks/order/heavy/categories; carrier presets valid) + gated integration (idempotent load, settings singleton). Verified live: migrate deploy + db:seed twice → 30 questions, 10 sensitive, 1 settings row; all 21 tests green with `TEST_DATABASE_URL`. — code-writing agent (Claude)
- **2026-06-22 — Phase 0, step 0.4 (Prisma schema + initial migration).** Modeled the 19 core entities and their enums from spec §5 in `apps/api/prisma/schema.prisma` (account, partnership, partner, session, question, clause, clause_signoff, clause_version, share_calculation, matrix_decision, matrix_cell, legal_mapping, consistency_finding, questionnaire_invite, questionnaire_answer, client_access, client_artifact, export_record, settings) — UUID PKs, snake_case mapping, FK indexes, sensible cascades. Authored the initial migration `20260622120000_init_core_schema` via `prisma migrate diff` (enables the `vector` extension idempotently; added a CHECK constraint for matrix level 1..5). AI/ASR tables (audio/transcript/kb/ai_job) deferred to Phases 7–8. Added `db:migrate`/`db:deploy`/`db:reset` scripts and a gated integration test (`schema.integration.test.ts`, runs only with `TEST_DATABASE_URL`). Verified: `migrate deploy` applies on the init-script DB and on a brand-new DB; `migrate status` clean; integration test (connect + core tables + pgvector) green; lint/format/typecheck/build/test all green. — code-writing agent (Claude)
- **2026-06-22 — Phase 0, step 0.3 (NestJS API skeleton).** Added the `@psa/api` NestJS app: `ConfigModule` with zod-based env validation (`src/config/env.ts`), `PrismaModule`/`PrismaService` (lifecycle-bound client), `HealthModule` with `GET /health` (liveness, 200) and `GET /health/db` (readiness, 200 up / 503 down), a global `AllExceptionsFilter`, and `main.ts` bootstrap. Prisma schema is minimal (datasource + generator + pgvector preview) and **co-located at `apps/api/prisma/`** (removed the root `prisma/`) for pnpm-friendly client resolution. Added `apps/api/Dockerfile` (single-stage) and wired the `api` service in `deploy/docker-compose.yml`. Unit tests for `validateEnv` and `pingDatabase` (Vitest). Decisions: zod is the single validation library (Nest `ValidationPipe`/class-validator intentionally not added; a zod request pipe comes in 0.6); `PRISMA_GENERATE_SKIP_AUTOINSTALL=true` baked into api scripts + Dockerfile to avoid Prisma's nested package-manager call. Verified: lint/format/typecheck/build/test green; API live-run via Node (health 200, db-down 503) and as a Docker container against the compose db. — code-writing agent (Claude)
- **2026-06-22 — Phase 0, step 0.2 (dev infrastructure).** Added `deploy/docker-compose.yml` with a `db` service (PostgreSQL 16 + pgvector, named volume, healthcheck), `deploy/.env.example` (POSTGRES_*/DATABASE_URL/secret placeholders; the required password has no default), and `deploy/db/init/01-enable-extensions.sql` (enables `vector` on a fresh volume). `api`/`web` are committed as commented templates, activated in 0.3/0.7 once their Dockerfiles exist. Live-verified: `docker compose up -d db` → Postgres ready, pgvector 0.8.3 enabled in the `psa` DB; `down -v` clean. — code-writing agent (Claude)
- **2026-06-22 — Phase 0, step 0.1 (monorepo & tooling baseline).** Stood up the pnpm monorepo (`apps/web`, `apps/api`, `packages/shared`) with TypeScript (strict), ESLint 9 (flat config) + Prettier, and Vitest. Added root scripts (`build`/`lint`/`typecheck`/`test`/`format`), the Appendix-D directory skeleton (`prisma/`, `seed/`, `resources/`, `deploy/`, `tests/` with purpose READMEs), and the first shared primitive (`USER_ROLES` + guard) with tests. Updated CLAUDE.md/AGENTS.md/README to drop the "concept stage / no tooling" framing. All checks green. — code-writing agent (Claude)
- **2026-06-22 — Adopted the MVP spec and composed the Phase 0 plan.** Added `docs/spec/psa-mvp.md` (the authoritative ТЗ), ADR `docs/decisions/0001-adopt-psa-mvp-spec-and-stack.md`, and `docs/plans/phase-0-skeleton-and-infrastructure.md`. No application code yet. — code-writing agent (Claude)
- **2026-06-21 — Established the agent collaboration & handoff system.** Added the "Collaboration and handoff" and "Branching and commits" sections to `AGENTS.md`, created this `HANDOFF.md`, and added a pointer from `CLAUDE.md`. Switched the workflow to direct commits on `main` (no pull requests). — code-writing agent (Claude)

## Backlog / next up

Base order = the phased critical path in spec §11 (core = Phases 0–6; AI 7 / ASR 8 deferrable). **Re-sequenced by the demo-first re-plan (owner decision 2026-07-02, `docs/plans/demo-readiness.md`):** the agreement-assembly/export part of Phase 4 is pulled forward (steps D3/D4); Phases 2 and 3 run after the demo feedback.

- **Phase 0** — Skeleton & infrastructure — DONE, live.
- **Phase 1** — Cases, sessions, scenario engine, capture (§4.2–4.4) — DONE, live.
- **Demo readiness D1–D5** (active plan above) — session-day UX; manual shares input (FR-5.1); agreement assembly + print; PDF/DOCX export (part of §4.8/§10); demo kit. In parallel: **Watson backlog W1–W6** (backups, registration gating, password change, e2e suite, sanitization, health cron).
- **Демо Грицу и архитекторам** → feedback → re-prioritize the rest.
- **Phase 2** — Gritz Calculator (§4.5, Appendix E) — after the demo (interim: manual shares input from D2).
- **Phase 3** — Authority matrix (§4.6) — after the demo (owner may pull it earlier, decision O3).
- **Phase 4 (rest)** — Legalization checklist + its export CSV (§4.8 FR-8.3–8.4, §10 DOC-2).
- **Phase 5** — Completeness checks + lifecycle/versions/diff (§4.7 FR-7.1, §4.9).
- **Phase 6** — Client portal (§4.11).
- **Phase 7** — AI assistant (text): inference, draft/rephrase/AI-checks, RAG, job queue (§4.10, §6).
- **Phase 8** — ASR (§4.10 FR-10.6).
