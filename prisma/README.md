# prisma/

Prisma schema (`schema.prisma`) and migrations for the psa PostgreSQL database.

Populated in Phase 0 step 0.4 (core entities from spec §5 + the `vector`
extension; AI/ASR tables are deferred to their phases). Empty in step 0.1.
